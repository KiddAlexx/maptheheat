import UserProfile from '@/features/userProfile/components/UserProfile';

function Profile() {
  return (
    <main className="p-10">
      <UserProfile />
    </main>
  );
}

export default Profile;
